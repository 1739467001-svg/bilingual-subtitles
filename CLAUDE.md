# CLAUDE.md

## 项目目的 (Purpose)

本仓库用于把 **LRC 字幕文件** 转换成 **英文 + 中文双语 SRT 字幕文件**。

用户会发来 LRC 文件（作为文件，或直接粘贴内容），Claude 负责解析时间轴、翻译，
并把生成的双语 SRT 保存到本仓库。

## 输出格式规范 (Output format spec)

转换结果是标准 SRT 文件，约定如下：

1. **双语排版**：每条字幕**第一行英文（原文）、第二行中文（翻译）**。

   ```
   1
   00:00:01,000 --> 00:00:04,500
   Everything you need to know about this World Cup
   关于这届世界杯你需要知道的一切
   ```

2. **文件命名**：`<原文件名>_英中双语.srt`
   - 例：`World_Cup_2026_英中双语.srt`、`Michelob_ULTRA_Superior_Match_英中双语.srt`

3. **时间轴 (timing)**：
   - LRC 的 `[mm:ss.xx]` 时间戳作为每条字幕的**开始时间**。
   - 每条字幕的**结束时间 = 下一条的开始时间**（字幕连续显示，中间无空隙，
     一直显示到下一句出现为止）。
   - **最后一条**：结束时间 = 开始时间 + 约 5 秒。
   - 时间格式换算：LRC 的 `[mm:ss.xx]`（百分之一秒）转成 SRT 的
     `HH:MM:SS,mmm`（毫秒）。例：`[01:23.45]` → `00:01:23,450`。

4. **编号**：从 `1` 开始连续编号。

5. **编码**：UTF-8（不带 BOM），换行用 `\n`。

## 翻译规范 (Translation guidelines)

- 忠实、自然、口语化的中文，符合字幕阅读习惯，单行不要太长。
- 专有名词（人名、队名、品牌名）保留原文或采用通用译名。
  例：Michelob ULTRA 这类品牌名保留 "Ultra"。
- 注意双关、俚语、文化梗；必要时在**交付说明**里点出来（**不要**写进字幕正文）。
  例：啤酒广告里的 "ultras" 是一语双关——既指 Michelob ULTRA，也指死忠球迷（ultras）。
- 识别并修正明显的转写/听写错误。例："NVAR" 实为 "VAR"（视频助理裁判）。

## 存放位置 (Where files go)

转换后的双语 SRT 放在 `subtitles/` 目录下。

## 工作流程 (Workflow)

1. 用户发来 LRC 文件或粘贴 LRC 内容。
2. Claude 按上面的规范解析时间轴 + 翻译，生成 `<原文件名>_英中双语.srt`。
3. 保存到 `subtitles/`，提交（commit）并推送（push）到当前工作分支。
4. 如用户需要，也可通过聊天直接把生成的文件发给用户。

> 注意：本仓库（`bilingual-subtitles`）是有写入权限的，可以正常 push。
> 不要再往 `Food-Scroll` 等其他仓库推送（那是历史会话里推错的目标，会 403）。
